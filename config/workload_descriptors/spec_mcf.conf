# Workload descriptor: spec/mcf
# Single source of truth for this workload's measured peak RSS
# (consumed by run_characterization.sh USE_PEAK_RSS_RATIO sizing).
# Single-invocation.  Wall clock ~13:46 per run (no instrumentation,
# NUMA node 0, 2026-06-25).
suite=spec
workload=mcf
peak_rss_kb=623744   #   ~609 MB  (2026-06-11)

# Workload descriptor: spec/deepsjeng
# Single source of truth for this workload's measured peak RSS
# (consumed by run_characterization.sh USE_PEAK_RSS_RATIO sizing).
# Single-invocation.  Wall clock ~10:17 per run (no instrumentation,
# NUMA node 0, 2026-06-25).
suite=spec
workload=deepsjeng
peak_rss_kb=716640   #   ~700 MB  (2026-06-25)

# Workload descriptor: graph500
# Single source of truth for this workload's measured peak RSS
# (consumed by run_characterization.sh USE_PEAK_RSS_RATIO sizing).
suite=graph500
workload=graph500
peak_rss_kb=35891272 # ~36 GB  (2026-06-24)
# Elapsed (wall clock) time (h:mm:ss or m:ss): 4:29.10

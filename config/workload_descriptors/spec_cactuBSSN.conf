# Workload descriptor: spec/cactuBSSN
# Single source of truth for this workload's measured peak RSS
# (consumed by run_characterization.sh USE_PEAK_RSS_RATIO sizing).
# Single-invocation.  Wall clock ~7:07 per run (no instrumentation,
# NUMA node 0, 2026-06-11).
suite=spec
workload=cactuBSSN
peak_rss_kb=808640   #   ~789 MB  (2026-06-11)

# Workload descriptor: spec/lbm
# Single source of truth for this workload's measured peak RSS
# (consumed by run_characterization.sh USE_PEAK_RSS_RATIO sizing).
# Single-invocation.  Wall clock ~6:05 per run (no instrumentation,
# NUMA node 0, 2026-06-11).
suite=spec
workload=lbm
peak_rss_kb=420480   #   ~410 MB  (2026-06-11)

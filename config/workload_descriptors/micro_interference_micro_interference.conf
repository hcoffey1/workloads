# Workload descriptor: micro_interference/micro_interference
# Single source of truth for this workload's measured peak RSS
# (consumed by run_characterization.sh USE_PEAK_RSS_RATIO sizing).
suite=micro_interference
workload=micro_interference
peak_rss_kb=8421056   # ~8.0 GB (2026-05-21)

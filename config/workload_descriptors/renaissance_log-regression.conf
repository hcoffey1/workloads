# Workload descriptor: renaissance/log-regression
# Single source of truth for this workload's measured peak RSS
# (consumed by run_characterization.sh USE_PEAK_RSS_RATIO sizing).
suite=renaissance
workload=log-regression
peak_rss_kb=4800060   #  ~4.6 GB  (2026-05-25)
